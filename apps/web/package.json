{
  "name": "@siren/web",
  "version": "0.1.0",
  "private": true,
  "description": "Siren browser application",
  "license": "GPL-3.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@siren/core": "workspace:*"
  },
  "devDependencies": {
    "vite": "^6.0.7",
    "vitest": "^4.0.16"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
