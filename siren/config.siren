task project-config {
    description = "Allow setting project-level maxDepth for dependency traversal"
    # Depends on the config-grammar entry (see siren/language-features.siren)
    depends_on = config-grammar
}
