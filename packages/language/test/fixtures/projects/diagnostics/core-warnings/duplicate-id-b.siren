# W003 — Duplicate entry ID (second file)
# Second occurrence of duplicate IDs — these are dropped by the builder
task duplicate-task {
    description = "Second occurrence - this one is dropped"
}

milestone duplicate-milestone {
    description = "Second milestone occurrence - dropped"
}
