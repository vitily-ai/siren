{
  "name": "@sirenpm/grammar",
  "private": true,
  "scripts": {
    "build": "tree-sitter build --wasm",
    "generate": "tree-sitter generate",
    "test": "yarn run generate && yarn run build && tree-sitter test --show-fields"
  },
  "devDependencies": {
    "tree-sitter-cli": "^0.26.8"
  }
}
