{
  "name": "tree-sitter-siren",
  "version": "0.1.0",
  "description": "Tree-sitter grammar for Siren PMaC language",
  "main": "grammar.js",
  "scripts": {
    "build": "tree-sitter generate && tree-sitter build-wasm",
    "test": "tree-sitter test"
  },
  "tree-sitter": [
    {
      "scope": "source.siren",
      "file-types": ["siren"]
    }
  ],
  "devDependencies": {
    "tree-sitter-cli": "^0.23.2"
  }
}
